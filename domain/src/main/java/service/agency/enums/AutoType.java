package service.agency.enums;

public enum AutoType {
        SUV,
        HATCHBACK,
        CROSSOVER,
        CONVERTIBLE,
        SEDAN,
        SPORTS,
        COUPE,
        MINIVAN,
        PICKUP
}
